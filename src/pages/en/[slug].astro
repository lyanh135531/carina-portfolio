---
import ServiceDetailPage from "@/components/ServiceDetailPage.astro";
import type { ServiceDetail } from "@/data/serviceDetails";
import { getServiceDetailStaticPaths } from "@/data/serviceDetails";

export const getStaticPaths = getServiceDetailStaticPaths;

type Props = Readonly<{
  detail: ServiceDetail;
}>;

const { detail } = Astro.props;
---

<ServiceDetailPage locale="en" detail={detail} />
